首先我们要写一些一维perlin噪音和二维perlin噪音的函数作为数学工具
（输入是随机数种子，perlin噪音的基本参数，和需求输出的大小，其中二维还要求可以旋转）

地图是有边界的！不用无休止的加载地块
首先确定地图范围为 x: -2000~2000, y:-700~300，以 y=0 作为平面

首先是第一步，生成地平线：
首先利用多个正弦波叠加生成，再叠加几个小的一维perlin噪音来确定地表，然后利用二维噪音不规则地将靠近地表的石头转化为泥土，并且让上面没方块的泥土变为草方块

然后是简单的洞穴生成：
首先先算两个二维柏林噪音叠加值（都可以施加一定的旋转）：
A噪音：间隙较大，幅度较大，多个叠加，控制整体洞穴的产生
B噪音：间隙较小，幅度较小，多个叠加，控制不规则的洞穴边界
A - B，然后差值大于一定比例的会变成空气

对于每个chunk，让A-B 的阈值为当前chunk（以及部分旁边chunk，权重较小）的平均值稍微大一点（尽量让10%的地方是洞穴），并且chunk间平滑过渡
泥土层也可以生成洞穴，但是泥土层附近的洞穴会明显较少

先做到这里为止
给了个最终效果参考（泰拉瑞亚游戏原图）